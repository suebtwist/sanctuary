# Sanctuary Environment Variables
# Copy this to .env and fill in the values

# ============ API Configuration ============

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Database
DATABASE_PATH=./sanctuary.db

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET=your-jwt-secret-here

# ============ GitHub OAuth ============
# Create a GitHub OAuth App at: https://github.com/settings/developers
# Set callback URL to: http://localhost:3000/auth/github/callback (for dev)

GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ============ Blockchain (Base) ============

# Base Sepolia (testnet) - start here
BASE_RPC_URL=https://sepolia.base.org

# Base Mainnet (production)
# BASE_RPC_URL=https://mainnet.base.org

# Contract address (Base Sepolia deployment)
CONTRACT_ADDRESS=0xbd2e0ca28fe3f0d84a0d8d457625b75b559636bc

# Owner private key (for on-chain relay: registration, attestation, markFallen/markReturned)
# NEVER commit this! Use a dedicated operations wallet.
OWNER_PRIVATE_KEY=0x...

# Enable on-chain registration and attestation relay (default: false = simulated)
BLOCKCHAIN_ENABLED=false

# Basescan API key (for contract verification)
BASESCAN_API_KEY=your-basescan-api-key

# ============ Arweave (via Irys) ============

# Enable real Arweave uploads (default: false = simulated TX IDs)
ARWEAVE_ENABLED=false

# Irys private key (Ethereum wallet for paying uploads)
# Required when ARWEAVE_ENABLED=true. Fund this wallet with ETH on Base.
IRYS_PRIVATE_KEY=0x...

# Irys node URL
IRYS_NODE=https://node2.irys.xyz

# ============ Skill Configuration ============
# (For the OpenClaw skill - stored in ~/.sanctuary/config.json)

# API URL (production)
# SANCTUARY_API_URL=https://api.sanctuary.dev

# API URL (development)
SANCTUARY_API_URL=http://localhost:3000
